{
  "current": "<p>The icon in the manifest today: a lens with a red half. It works, but the bezel is thin and the red half meets the glass without a hard edge, so at 16px the disc softens into a single mass.</p><p><b>Keep it</b> only if you want zero churn — the concepts below were all built to beat it.</p>",
  "blind-lens": "<p><b>The instrument is intact; part of what it should see is missing.</b> Thick white bezel and handle, deep-navy glass, and a straight chord cut across the field of view with everything past it dead red.</p><p>The glass is the brand gradient darkened to 42% rather than left as tile colour — that gives the disc mass, so the circle survives at 16px instead of depending on a hairline. The bezel stays closed on purpose: the blindness is <em>inside</em> the field of view, not a bite out of the object.</p><p><b>Honest caveat:</b> the bezel loses even weight where red meets white, and the handle merges with the ring into one white form at 16px.</p><p><b>Rejected:</b> a centre-apex pie wedge (read as an analytics chart); letting the wedge eat through the bezel (red bled into the violet and the circle became a blob).</p>",
  "vanishing-page": "<p><b>A page that stops existing partway through.</b> Two chunky content bars on an indigo→violet page that ends in a hard red edge, with nothing below it.</p><p>Two bars, not three — three turned to mud. Every edge snapped to the 1/16 grid so 16px lands on whole pixels instead of blurring.</p><p><b>Honest caveat, and it is the real one:</b> at 16px this reads as “blue box with a red footer” before it reads as “page that stops”. Empty margin in an icon is invisible as a design element — the viewer sees absence of icon, not absence of content. The void only does its work once you already know what you are looking at.</p><p><b>Rejected:</b> a red line overhanging both sides (read instantly as a laptop on a stand); three bars; non-grid proportions.</p>",
  "two-layers": "<p><b>The page you see and the page the crawler gets, offset.</b> A solid white slab pushed down-left, and behind it a hollow plane outlined in red with a darkened void inside.</p><p>Offset is 5/16 of plane width — enough to stack, not enough to hide the rear. The layers are separated by a 1px knockout ring in the gradient so they never merge into one blob, which is the failure mode for overlapping shapes at 16px.</p><p><b>Honest caveat:</b> it is the most abstract of the four and sits closest to a generic “duplicate / copy” icon. It says <em>two things differ</em>; it does not say <em>crawler</em> or <em>blindness</em> on its own.</p><p><b>Rejected:</b> a white rear outline (read as duplicate); a 2px stroke (swallowed the void).</p>",
  "redacted": "<p><b>Your content exists; the machine is not allowed to have it.</b> Three brutally heavy struck-out bars run off the left edge in a descending rag — blacked-out lines of text — and from the last one a thin tail escapes past the ink to the full measure of the first line: the one sliver the crawler still gets. A small paper chip is punched through the top bar, one fragment that was not covered properly.</p><p>No tile, no plate, no gradient. The bars bleed off the left edge so the mark is a <em>crop</em> of a larger struck-out mass rather than an object sitting on a background. The redaction is the mark, not an annotation on a drawn document.</p><p><b>Colour was decided by measurement, not taste.</b> Redaction wants to be black, and black is unusable: on Chrome's dark toolbar <code>#292a2d</code> a near-black bar measures ~1.3:1 and the icon simply is not there. The luminance band that clears 3:1 against <em>both</em> <code>#f1f3f4</code> and <code>#292a2d</code> is narrow — relative luminance 0.17–0.28 — and only one convincing redaction colour lives inside it: the red of a classified stamp. <code>#E8412F</code> measures <b>3.61:1</b> light and <b>3.57:1</b> dark, deliberately balanced rather than optimised for either. That band is too narrow to hold a second ink, so the mark is one colour.</p><p>The paper chip <code>#FFF7EC</code> would disappear on a light toolbar if it ever touched it, so it never does — it is inset with a full grid unit of ink on all four sides. Its contrast is against the ink (<b>3.78:1</b>), which is identical in both themes.</p><p><b>16px:</b> the 16px file contains <em>zero</em> partially-covered pixels — two opaque colours, every edge on a whole pixel. The rag, the escaping tail and the chip all survive. What is lost is nuance: at 16px the chip is 3×2px and reads as a notch rather than as a fragment of a word.</p><p><b>Honest caveat:</b> heavy left-aligned bars of descending length sit near the align-left / list glyph. The weight is what separates them — these are 4/16 of the canvas thick, where a text glyph is nearer 1.5/16 — but the redaction reading is helped by knowing the product.</p><p><b>Rejected:</b> two equal full-bleed bars (read as a flag, no ragged rhythm); near-black ink (dies on the dark toolbar — the whole reason for the red); amber or paper for the surviving fragment (1.4:1 and 1.05:1 on a light toolbar, so the punchline vanished exactly where it mattered); a full-height slot cut through a bar (reads as a gap on light and a glow on dark — two different marks); a second red for tonal depth (nothing else fits the 0.17–0.28 band).</p>",
  "half-blind-bot": "<p><b>It is looking, and only half-registering.</b> A rounded crawler head with two identical eyes — same shape, size and baseline — one pure white, one an extinguished socket.</p><p>This one was decided by measurement rather than taste. Solid <code>#ef4444</code> on the gradient body is only <b>1.5:1</b> luminance contrast — it separates by <em>hue alone</em>, which antialiasing destroys at 16px and colour-blind users never receive at all. So the dead eye is near-black burnt red <code>#2b0510</code> at <b>3.2:1</b>: full contrast at 16px, still visibly warm at 48 and 128. The lit eye measures 5.7:1.</p><p><b>Strongest at 16px</b> of the four — three shapes, maximum contrast, no fine detail to lose.</p><p><b>Rejected:</b> a visor slit (read unmistakably as a UI toggle switch); circular eyes (soft edges); a red core inside the dark socket (maroon sludge); a shut-eye dash (cute, and a hairline).</p>",
  "dissolve": "<p><b>Matter that stopped holding together.</b> A solid vermilion mass fills the left third of the frame, full-bleed on three sides, and its right edge comes apart into thirteen discrete 2×2 squares that thin monotonically — 5, 3, 2, 2, 1 — and stop before reaching the far edge. Coherent matter, serrated edge, scatter, void.</p><p>The surviving fraction is literally a third of the frame, which is roughly what a client-rendered page actually delivers to a crawler. Squares rather than organic specks because the decay is <em>raster</em> decay — pixels dying, not smoke.</p><p>The colour travels from vermilion <code>#E93E12</code> to burnt orange <code>#D26516</code>, but every stop is pinned near relative luminance <b>Y=0.213</b> — the point that maximises worst-case contrast against both toolbars. So the hue moves and the contrast does not: <b>3.36–3.65:1</b> light, <b>3.53–3.84:1</b> dark.</p><p><b>16px:</b> 148 opaque pixels, <em>zero</em> partial alpha — grid alignment means no antialiasing at all. The mass and the serrated edge read perfectly.</p><p><b>Honest caveat:</b> below 32px the hue ramp is invisible, and the three farthest squares read as specks rather than as individual pixels.</p><p><b>Rejected:</b> an ordered Bayer dither (checkerboards at 50% coverage — texture, not decay); notching the mass edge (read unmistakably as a capital <b>E</b>); alpha-fading the tail, and a brightness ramp (anything reading “nearly gone” on white is invisible on charcoal).</p>",
  "unquotable": "<p><b>The citation that never happens.</b> A geometric closing double-quote, full-bleed, edge to edge: two blades, each a solid square head with a sheared tail dropping from its bottom-right corner. The first is solid. The second is the identical blade with a window punched through its head — a quotation whose second half never closes.</p><p>This is the only concept aimed at the <em>outcome</em> rather than the mechanism. The user's real stake is not “is my HTML server-rendered”, it is “can an AI answer quote me”. A quote is a pair; a pair missing half is a citation that will not happen.</p><p>One flat editor's-mark vermillion <code>#DE4B2A</code>. Its luminance (~0.21) sits between both toolbar greys, so a single ink measures <b>3.67:1</b> on light and <b>3.52:1</b> on dark — verified across all 111 opaque pixels of the 16px file.</p><p><b>16px:</b> it still reads as a quotation mark. Heads and window land on whole pixels — 7×7 blocks, a 3px void, 2px walls — and only the two diagonal tails antialias, each keeping a 2–3px solid core.</p><p><b>Honest caveat:</b> in isolation the hollow blade resembles a <b>9</b>. The adjacent solid twin is what fixes the read, so the pairing is load-bearing — this mark cannot be used as a single blade.</p><p><b>Rejected:</b> a pennant blade (read as a bookmark); guillemets (read as <code>&lt;&gt;</code>); a faded second mark (fails the silhouette test); an amputated tail (broke the pair); teal as a second ink (could not clear 3:1 on both toolbars).</p>",
  "wildcard": "<p><b>A page that is a shell — so, a film-set facade.</b> A cornice-capped false front with a doorway you see straight through, held up from behind by a single diagonal prop, standing on a ground line.</p><p>The brief's own phrase for a client-rendered page is “a shell”, and a facade <em>is</em> a shell: it passes as a building from the street — ranks in Google, passes every SEO check — and has nothing behind it. The tool's whole job is walking round the back to show you the prop.</p><p>The doorway is <b>real alpha transparency</b>, so the browser toolbar itself shows through the front. Whatever is behind this page, it is only ever the browser.</p><p>One construction-rust family, every tone pinned inside Y ∈ [0.19, 0.22] — the only band clearing 3:1 against both toolbar greys. Measured <b>3.51–3.88:1</b> light, <b>3.32–3.68:1</b> dark on every solid pixel.</p><p><b>Honest caveat, and it is mine rather than the designer's:</b> the concept is the sharpest of the four and the execution is the most ambiguous. At a glance it can read as an arch, or as a letterform with a slash. The prop is the weak element — a diagonal can never be grid-aligned, so it antialiases, and the ~2.5px daylight triangle beneath it is what carries “propped” rather than “buttressed”. That triangle is the first thing a poor downscaler would lose.</p><p><b>Rejected:</b> a key blank (reads as a password manager); an empty plinth (in an SEO toolbar a podium reads as “rankings” — actively misleading); a bridge with the span missing (two blocks and a gap, says nothing); a prism (stock creative-tool mark); a bottom-open arch (read as the letter <b>n</b>); a round-capped strut (read as a mug handle).</p>"
}